/**
 * @description 从对象中挑选值
 * @param obj
 * @param keys
 * @returns
 *
 * @example
 *
 * ```ts
 * pick({a: "1", b: "b"}, ['a']) // => {a: 'a'}
 * ```
 */
export declare function pick(obj: object, keys: string[]): {};
